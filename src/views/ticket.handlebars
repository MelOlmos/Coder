<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticket</title>
</head>
<body>
    <h1>Ticket de compra</h1>
    <p><strong>Código:</strong> {{createdTicket._doc.code}}</p>
    <p><strong>Fecha de Compra:</strong> {{createdTicket._doc.purchase_datetime}}</p>
    <p><strong>Comprador:</strong> {{createdTicket._doc.purchaser}}</p>
    <p><strong>Total:</strong> {{createdTicket._doc.amount}}</p>

    <h2>Productos comprados</h2>
    <ul>
        {{#each products}}
            <li>{{this._doc.product._doc.title}} | Cantidad: {{this._doc.quantity}}</li>
        {{/each}}
    </ul>
    

    {{#if productsNotPurchased.length}}
    <h2>Productos sin stock</h2>
    <ul>
        {{#each productsNotPurchasedTitle}}
            <li>Nombre: {{this}}</li>
        {{/each}}
    </ul>
    {{/if}}

</body>
</html>